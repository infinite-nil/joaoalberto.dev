import { styled } from "@/ui/config/theme";

const Title = styled("h1", {
  color: '$green11',
  fontFamily: "$heading",
  fontSize: "$heading",
  fontWeight: "bold",
  margin: "0",
});

export default Title;
