---
import Heading from "./Heading.astro";
import { SITE_TITLE } from "../config";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<header>
  <Heading>{title || SITE_TITLE}</Heading>
</header>
