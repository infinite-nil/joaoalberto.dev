---
export interface Props {
  classNames?: string;
}

const { classNames = "" } = Astro.props;
---

<h1
  class={`font-medium leading-6 text-gray-900 dark:text-gray-300 ${classNames}`}
>
  <slot />
</h1>
