---
export interface Props {
  classNames?: string;
}

const { classNames = "" } = Astro.props;
---

<p
  class={`font-medium text-xs leading-[18px] text-zinc-400 md:text-gray-400 ${classNames}`}
>
  <slot />
</p>
